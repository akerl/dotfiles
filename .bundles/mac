fpath=(~/.completions /opt/boxen/homebrew/share/zsh-completions /opt/boxen/homebrew/share/zsh/functions /opt/boxen/homebrew/share/zsh/site-functions)

lockfile -1 -r5 ~/.zshrc-lock
source ~/.bundles/gpg-agent
source ~/.bundles/rbenv
rm -f ~/.zshrc-lock

alias sha512sum='shasum -a 512'
alias sha256sum='shasum -a 256'
alias ls='ls -G'

alias c='hss'
alias ccp='scp -S hss'
export HSS_PASS='ssh'

alias g='gist -c'

alias s='ssh'

alias gpom='git push origin master'
alias gppm='git push public master'
alias gpam='git push public master ; git push origin master'

alias gpg='gpg --homedir /Volumes/akerl-vault/gnupg'
alias keybase='keybase --homedir /Volumes/akerl-vault'

function hiddenfiles () {
    current="$(defaults read com.apple.finder AppleShowAllFiles)"
    if [ "$current" = "0" ] ; then
        echo "Now showing hidden files"
        new="true"
    else
        echo "Now hiding hidden files"
        new="false"
    fi
    defaults write com.apple.finder AppleShowAllFiles -bool $new
    killall Finder
}

export SSL_CERT_FILE=/Users/akerl/.sslcerts

export GOPATH=$HOME/.go

alias afk="/System/Library/CoreServices/Menu\ Extras/User.menu/Contents/Resources/CGSession -suspend"
alias emptytrash="sudo rm -rfv /Volumes/*/.Trashes; sudo rm -rfv ~/.Trash; sudo rm -rfv /private/var/log/asl/*.asl"

